INITIALIZE one variable to the integer 7 and a second variable to the integer 6.
Then PRINT the PRODUCT of those variables.

















Define a FUNCTION called first_letter. It take a word string as an argument and return the first letter of the word.
for example
first_letter('mario') will return 'm'
